def test_answer_question(answered_questions):
    """
    Tests that the answered_questions fixture is working correctly.
    """
    assert answered_questions is None # The fixture asserts success internally
